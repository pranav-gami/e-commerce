<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>PrimeStore</title>
    <link rel="shortcut icon" href="/assets/media/logos/favicon.ico" />
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap');

        body {
            font-family: 'Roboto', sans-serif;
            background-color: #f0f4f8;
            margin: 0;
            padding: 0;
            min-height: 100vh;
        }

        .container-wrapper {
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 35px 0 20px 0;
        }

        .container {
            position: relative;
            background-color: #fff;
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            max-width: 600px;
            width: 100%;
            text-align: center;
        }

        /* .home-button {
            position: absolute;
            top: 25px;
            left: 30px;
            color: #565656;
            border: none;
            font-size: 18px;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .home-button:hover {
            color: rgb(70, 70, 255);
        } */

        .success-icon {
            font-size: 60px;
            color: #28a745;
            margin-bottom: 20px;
        }

        h1 {
            font-size: 28px;
            color: #28a745;
            margin-bottom: 10px;
        }

        p {
            font-size: 18px;
            color: #555;
            margin-bottom: 30px;
        }

        .details {
            text-align: left;
            background-color: #f9f9f9;
            padding: 20px;
            border-radius: 8px;
            margin-bottom: 30px;
        }

        .details p {
            margin: 10px 0;
            font-size: 16px;
        }

        .details strong {
            color: #333;
        }

        .actions {
            display: flex;
            justify-content: center;
            gap: 20px;
        }

        .view_details-btn,
        .continue-btn {
            padding: 12px 24px;
            font-size: 16px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .view_details-btn {
            background-color: #37b554;
            color: white;
        }

        .continue-btn {
            background-color: #fa788b;
            color: white;
        }

        .view_details-btn:hover {
            transform: translateY(-2px);
            background-color: #29a446;
        }

        .continue-btn:hover {
            transform: translateY(-3px);
            background-color: #f9657b;
        }

        @media (max-width: 768px) {
            .container {
                padding: 20px;
            }

            h1 {
                font-size: 24px;
            }

            p {
                font-size: 16px;
            }

            .actions {
                flex-direction: column;
                gap: 10px;
            }

        }
    </style>
</head>

<body>
    <div class="container-wrapper">
        <div class="container">
            <div class="success-icon">✔</div>
            <h1>Payment Successful!</h1>
            <p>Thank you for your purchase. Your payment has been processed successfully.</p>
            <div class="details">
                <p><strong>Order Number:</strong> #123456</p>
                <p><strong>Transaction ID:</strong> TXN789012</p>
                <p><strong>Amount Paid:</strong> $150.00</p>
                <p><strong>Date:</strong> October 10, 2023</p>
            </div>
            <div class="actions">
                <button class="view_details-btn" onclick="redirectToProfile()">View Order Details</button>
                <button class="continue-btn" onclick="redirectToHome()">Continue Shopping</button>
            </div>
        </div>
    </div>

    <script>
        function redirectToHome() {
            window.location.href = '/primestore';
        }

        function redirectToProfile() {
            window.location.href = '/primestore/my/dashboard';
        }
    </script>
</body>

</html>